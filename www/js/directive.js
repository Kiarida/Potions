angular.module('starter.directive', [])

.directive('activation', function(){
    return {
        link: function(scope, elm){
        	/*console.log("BLABLA");
        	var result = document.getElementsByClassName("touched");
            var wrappedResult = angular.element(result);
            console.log(result);
            console.log('eee', elm, arguments);
            for(var i = 0; i< wrappedResult.length; i++){
            	//console.log(i);
            	console.log(wrappedResult[i]);
            	//wrappedResult[i].removeClass("touched");
            }
            
            elm.addClass('touched');*/
        }
    }
});